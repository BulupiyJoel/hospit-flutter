import 'package:flutter/material.dart';

class ListDonnor extends StatelessWidget {
  const ListDonnor({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    appBar: AppBar(title: Text("Liste donneurs de sang"),),
    body: Column(
    ),);
  }
}